import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      title: '8K Camera',
      theme: ThemeData.dark(),
      home: Scaffold(
        appBar: AppBar(title: const Text('My 8K Camera'), centerTitle: true),
        body: const Center(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              Icon(Icons.camera_alt, size: 100, color: Colors.white),
              SizedBox(height: 20),
              Text('8K Camera Ready!', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),
              SizedBox(height: 10),
              Text('APK Build Success', style: TextStyle(fontSize: 16)),
            ],
          ),
        ),
      ),
    );
  }
}
