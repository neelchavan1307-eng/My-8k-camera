name: my_8k_camera
description: 8K Pro Camera
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  gal: ^2.3.1
  permission_handler: ^11.3.1
  path_provider: ^2.1.2

flutter:
  uses-material-design: true
